refresh_rate: 30 seconds

appenders:
  stdout_json:
    kind: console
    encoder:
      kind: json
loggers:
  access:
    level: info
    appenders:
      - stdout_json
    additive: false

root:
  level: info
  appenders:
    - stdout_json
