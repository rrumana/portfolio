name: Build and Deploy Staging

on:
  push:
    branches:
      - v2
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/portfolio:staging
  HAS_KUBE_CONFIG: ${{ secrets.KUBE_CONFIG_STAGING != '' }}
  GHCR_USERNAME: ${{ secrets.GHCR_USERNAME || github.actor }}
  GHCR_TOKEN: ${{ secrets.GHCR_TOKEN || secrets.GITHUB_TOKEN }}
  USE_LOCAL_KUBECONFIG: true
  BUILDKIT_PROGRESS: plain

jobs:
  build-and-push:
    runs-on: [self-hosted, portfolio-dev]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ env.GHCR_USERNAME }}
          password: ${{ env.GHCR_TOKEN }}

      - name: Build and push staging image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile.staging
          push: true
          tags: ${{ env.IMAGE_NAME }}
          no-cache: true
          network: host

      # Optional: configure kubeconfig from secret if you don't want to rely on the runner's local kubeconfig
      - name: Configure kubeconfig (optional)
        if: ${{ env.HAS_KUBE_CONFIG == 'true' }}
        run: |
          mkdir -p "$HOME/.kube"
          echo "${KUBE_CONFIG_STAGING}" | base64 --decode > "$HOME/.kube/config"
        env:
          KUBE_CONFIG_STAGING: ${{ secrets.KUBE_CONFIG_STAGING }}

      - name: Restart staging deployment (optional)
        if: ${{ env.HAS_KUBE_CONFIG == 'true' || env.USE_LOCAL_KUBECONFIG == 'true' }}
        run: kubectl rollout restart -n dev deployment/portfolio-dev
